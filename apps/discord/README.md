# TiltCheck Discord bot (Phase 4 scaffold)

Minimal bot with `/vault status` placeholder.

## Run

```bash
DISCORD_BOT_TOKEN=... pnpm --filter @tiltcheck/discord dev
```

Register slash commands after the bot is invited with `applications.commands` scope.
